<%= form_with url: "/driver", method: :get do |f| %>
  <%= f.select("filter", options_for_select([["All", ""], ["To Do", "todo"], ["Done", "done"]])) %>
  <%= f.submit "Search" %>
<% end %>

<% @trips.each do |trip| %>
  <div style="border: 1px solid red;">
    <%= render trip %>
    <p>
      <%= link_to "Show this trip", trip %>
    </p>
  </div>
<% end %>

<%= link_to "New trip", new_trip_path %>

